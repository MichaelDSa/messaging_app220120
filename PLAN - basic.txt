0.1) create database with users
0.2) create tables:
	authentication table:
	CREATE TABLE authentication(
	id INT UNSIGNED NOT NULL AUTO_INCREMENT,
	username VARCHAR(255) NOT NULL,
	email VARCHAR(255) NOT NULL,
	password VARCHAR(255) NOT NULL,
	date TIMESTAMP NOT NULL DEFAULT CURRENT TIMESTAMP,
	PRIMARY KEY(id)
	)CHARACTER SET utf8

	authentication table (copy-paste):
CREATE TABLE authentication(id INT UNSIGNED NOT NULL AUTO_INCREMENT, username VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY(id)) CHARACTER SET utf8;


	> PHP will encrypt password before inserting data into table.
	> find out if PHP encrypt methods will ouput fewer than 255 char.

	messages table:
	CREATE TABLE $user_name(
	id INT UNSIGNED NOT NULL AUTO_INCREMENT,
	username VARCHAR(255) NOT NULL,
	participants VARCHAR(255) NOT NULL,
	speaker VARCHAR(255) NOT NULL,
	message TEXT NOT NULL,
	read TINYINT(1) UNSIGNED NOT NULL,
	date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
	PRIMARY KEY(id)
	)CHARACTER SET utf8

	> each user's table will be created on registration.
	 
	messages table (copy-paste)
CREATE TABLE $user_name(id INT UNSIGNED NOT NULL AUTO_INCREMENT, username VARCHAR(255) NOT NULL, participants VARCHAR(255) NOT NULL, speaker VARCHAR(255) NOT NULL, message TEXT NOT NULL, read TINYINT(1) UNSIGNED NOT NULL, date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY(id)) CHARACTER SET utf8

1) make header & footer pages
  -> ob_start() & ob_end_flush()
  -> <title><?PHP if define('TITLE') {print TITLE;} else { print 'The Message App';} ?> </title>s
2) make register user page
3) make login page


4) make chat app
  ->SESSION / COOKIE
--------above is done-----------
  ->function that sends message to both participants tables.


5) make a user settings page
  -> user can change password
  -> user can change email address
